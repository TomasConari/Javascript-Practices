//Unsolved
const arraySum = (nums1,nums2) => {
    let sums = [];
    for(let i = 0; i < nums1.length; i += 1){
        for(let x = 0; x < nums2.length; x += 1){
            if(Array.isArray(nums1[i])){

            }else if(Array.isArray(nums2[x])){

            }else{
            };
        };
    };
};