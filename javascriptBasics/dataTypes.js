//Data Types


    //String
        const name = "Tomás";
        console.log(`Name: ${name}`);

    //Interger
        const age = 19;
        console.log(`Age: ${age}`);

    //Boolean
        const isAdult = true;
        console.log(`Is an Adult?: ${isAdult}`);

    //Array
        const exampleArray = ["Tomás", 19, true, [1, 2, 3, 4]];
        console.log("Data Array: ", exampleArray);

    //Object
        const exampleObject = {
            name: "Tomás",
            lastname: "Contreras",
            age: 19,
            isAdult: true
        };
        console.log("Data Object: ", exampleObject);