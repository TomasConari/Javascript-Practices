const smash = (str) => {
    if(str.length > 1){
        return word += " " + str;
    }else{
        return word += str;
    };
};
const strings = ["H","e","l","l","o","How","Are","You"];
let word = "";
strings.forEach(smash);
console.log(word);